{
  "name": "cypress-cucmber-setup",
  "version": "1.0.0",
  "description": "BDD Framwork Cucumber Configuration Step by Step\r Cypress (12.11.0)\r Step#1\r Run the command  \"npm init\"\r Step#2 \r Run the command  \"npm install -g cypress\"\r Step#3\r Run the command \"\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cypress": "^12.11.0",
    "esbuild": "^0.17.18"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^17.0.0"
  }
}
